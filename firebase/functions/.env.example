# Copy to .env (deployed with functions) or .env.local (emulator only).
#
# Invite code for the private beta. If set, joinQueue requires it.
# Leave unset/empty to disable the gate.
RANKEDSAT_ACCESS_CODE=

# EMULATOR-ONLY test clock override in milliseconds (ignored in production
# builds; the code additionally requires FUNCTIONS_EMULATOR=true).
# RANKEDSAT_TEST_CLOCK_MS=4000
